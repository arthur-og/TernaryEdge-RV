config BR2_PACKAGE_NPU_TERNARIA
    bool "npu-ternaria"
    depends on BR2_LINUX_KERNEL
    help
      NPU Ternaria kernel module for Ternary Edge-RV.
      Platform driver with DMA, IRQ, and IOCTL interface.
