config BR2_PACKAGE_USER_APP
    bool "user-app"
    depends on BR2_PACKAGE_NPU_HAL
    help
      User-space inference application for Ternary Edge-RV.
      Uses NPU HAL to perform digit recognition on MNIST images.
